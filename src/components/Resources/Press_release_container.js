import React from "react";
import PressReleaseListItem from './press_release_list_item'

const PressReleaseContainer = props => {
  return (
    <div className='container'></div>
  );
};

export default PressReleaseContainer;
